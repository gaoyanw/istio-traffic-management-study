apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: httpbin-virtual-service
spec:
  hosts:
  - "*"
  gateways:
  - istio-system/istio-gateway
  http:
  - route:
    - destination:
        host: httpbin
        port:
          number: 8000
